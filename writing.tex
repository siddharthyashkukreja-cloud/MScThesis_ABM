


% ============================================================================
% ============================================================================
% FINAL EDITORIAL PASS  —  start-to-finish review of the compiled thesis.
% Locators are  [file : line]  against MSc_Thesis/chapters/*.tex (and main.tex).
% Tags: [SUBSTANTIVE] worth doing before submission · [CONSISTENCY] number/cross-ref
%       · [TYPO] · [POLISH]. Drafted replacement prose is given UNCOMMENTED between
%       "--- DRAFT ... ---" / "--- end DRAFT ---" markers so you can paste it straight in.
%       Nothing here is in the thesis yet — review and copy what you want.
% ============================================================================


%
% [CONSISTENCY] [:21] "Each stressed seed produces an average 8.3 client defaults" is under the REACTIVE
%   VaR margin; Table 5.1 then shows 14.8 at flat-4%. Disambiguate: change to
%   "Under the reactive VaR margin, each stressed seed produces an average 8.3 client defaults...".
% [CONSISTENCY] [:25-26] §5.1 uses the SLOIM fund (~$4.5B stress); §5.2 swaps to a fixed ~$12-14B fund.
%   Add a forward-pointer so the two fund sizes don't look contradictory:
%   "...this draws about 19\% of the pooled fund (the margin experiment of Section~\ref{sec:resh1}
%    substitutes a larger fixed default fund so its arms are comparable)."
% [POLISH] [:149] Paragraph head "Looking at the tail situations of a tail situation." -> e.g.
%   "\paragraph{A tail within a tail.}" (the current phrasing is clumsy).
% [TYPO] [:107] "it ask for a significantly lower amount" -> "it asks for".
% [CONSISTENCY] [:137] \section{A Synthetic Path Alternative} has NO \label. Add
%   \label{sec:res-synth} after it so the abstract/conclusion drafts above (which \ref it) resolve.
%   (sec:res-general at :6 and sec:resh1 at :69 are already labelled.)






%
% ============================================================================
% PRIORITY ORDER if you only have an hour: Abstract rewrite; §5.2.1 significance
% paragraph; §5.3 reconciliation sentence; bib (farmer 102(6), gao title, perello);
% then the typos. Everything else is polish.
% ============================================================================
